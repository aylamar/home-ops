---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/common-5.0.0/charts/library/common/values.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ganymede
spec:
  releaseName: *app
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
  maxHistory: 2
  install:
    remediation:
      retries: 3
  rollback:
    cleanupOnFail: true
  upgrade:
    force: true
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      ganymede:
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.4@sha256:5086f94abc783f1147d7c2a32c01db00ab594820026e4f6a82ac2af3dbde7fc7
            envFrom: &envFrom
              - secretRef:
                  name: ganymede-secret
        containers:
          app:
            image:
              repository: ghcr.io/zibbp/ganymede
              tag: 4.17.1@sha256:0aedfd8d2fccd8df986ab8b9d295a29413d177996c3f343d7f50fdcdd0722032
            env:
              TZ: America/Chicago
              PUID: 568
              PGID: 568
              MAX_CHAT_DOWNLOAD_EXECUTIONS: 1
              MAX_CHAT_RENDER_EXECUTIONS: 1
              MAX_VIDEO_DOWNLOAD_EXECUTIONS: 1
              MAX_VIDEO_CONVERT_EXECUTIONS: 1
              MAX_VIDEO_SPRITE_THUMBNAIL_EXECUTIONS: 1
              VIDEOS_DIR: /media/library/streams
              TEMP_DIR: /media/library/streams/tmp
            envFrom: *envFrom
            resources:
              requests:
                cpu: 100m
                memory: 4Gi
              limits:
                memory: 8Gi
    service:
      app:
        controller: *app
        ports:
          http:
            port: &port 4000
    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Downloads
          gethomepage.dev/name: ganymede
          gethomepage.dev/icon: twitch.svg
          gethomepage.dev/description: Twitch stream archiver
        hostnames: ["ganymede.${SECRET_DOMAIN}"]
        parentRefs:
          - name: internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: http
    persistence:
      config:
        enabled: true
        existingClaim: *app
        globalMounts:
          - path: /data/config
      logs:
        type: emptyDir
        globalMounts:
          - path: /data/logs
      media:
        type: nfs
        server: "192.168.10.42"
        path: /mnt/data/media
        globalMounts:
          - path: /media
